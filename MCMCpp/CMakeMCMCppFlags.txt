set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -std=c++11 -D_FILE_OFFSET_BITS=64")

if(UNIX AND APPLE)
    set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -stdlib=libc++ -DOSX_IS_ANNOYING")
    set(CMAKE_EXE_LINKER_FLAGS "${CMAKE_EXE_LINKER_FLAGS} -lc++")
else()
    set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -DGCC_SUPPORTS_FANCYNESS")
endif(UNIX && APPLE)